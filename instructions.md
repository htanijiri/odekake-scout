以下の3ファイルを修正してください。それ以外の部分は変更しないでください。

■ 1. plugins/odekake-scout/skills/planner/SKILL.md

「## 完了後」セクションを以下に変更:

## 完了後
【重要】全プランの出力が完了したら、メッセージの表示やユーザーへの確認は一切行わず、即座にadvance-scout Skillの手順（1〜2ヶ月先のイベント予約情報の調査）を開始せよ。「続けて〜」等のメッセージは絶対に表示しない。

■ 2. plugins/odekake-scout/skills/advance-scout/SKILL.md

以下の3点を修正:

A) フロントマターの user-invocable を変更:
  user-invocable: true
を以下に変更:
  user-invocable: true

※ user-invocable は true のままにする（単独でも呼べるようにしておく）

B) descriptionを以下に変更:
  description: >
    1〜2ヶ月先の大型イベントや予約が必要なイベントの先行情報を調査する。
    「来月のイベント予約を調べて」「先の予定を調べて」などで単独起動可能。
    また、planner Skillの完了後に自動的に実行され、
    今週末のレポートに先行予約情報も含めて出力する。

C) 「## ルール」セクションの後に以下のセクションを追加:

## 完了後
【重要】先行予約情報の出力が完了したら、メッセージの表示やユーザーへの確認は一切行わず、即座にoutput Skillの手順（最終レポートの生成）を開始せよ。「続けて〜」等のメッセージは絶対に表示しない。

■ 3. plugins/odekake-scout/skills/output/SKILL.md

A) 冒頭の説明を以下に変更:

おでかけスカウトの5つのスキル連鎖の最後。
scout（イベント調査）→ rating（選定）→ planner（計画作成）→ advance-scout（先行予約調査）→ **output（出力）**

B) 「## 出力形式」のMarkdownテンプレートに以下のセクションを追加（「📋 その他のイベント一覧」セクションと「ℹ️ 注意事項」セクションの間に挿入）:

## 🔮 来月の先行予約情報

### 🔴 予約締切が近いもの
{advance-scoutの🔴セクションをそのまま掲載}

### 🟡 予約受付中
{advance-scoutの🟡セクションをそのまま掲載}

### 🟢 予約開始前
{advance-scoutの🟢セクションをそのまま掲載}

### 📋 予約不要の注目イベント
{advance-scoutの📋セクションをそのまま掲載}

C) 「## ルール」セクションに以下を追加:
- advance-scoutの出力は内容を改変せず、セクション構造をそのまま統合レポートに含める
- 先行予約情報がない場合は「来月の先行予約情報」セクション自体を省略する
